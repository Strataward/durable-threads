interface:
  display_name: "Durable Threads"
  short_description: "Policy for native multi-agent coding"
  default_prompt: "Use $durable-threads to decide whether this work needs a worker and verify the result."

policy:
  allow_implicit_invocation: true
