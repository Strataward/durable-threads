# Cursor setup

`AGENTS.md` at the repository root is the canonical agent file. `CLAUDE.md` routes to it.

| Path | Role |
|---|---|
| `rules/durable-threads.mdc` | Always-on public plugin rules |
| `../.cursorignore` | Secrets and bulk paths |

When this folder is opened from the Durable AI workspace, also read `../../AGENTS.md` and `../../.cursor/rules/`.
